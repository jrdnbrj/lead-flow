# Copy to /etc/leadflow/ghcr.env on the VPS. Never commit the real file.
# Use a GitHub token limited to package read access for the private image.
# The production workflow can instead pass its short-lived GITHUB_TOKEN.
GHCR_USERNAME=
GHCR_READ_TOKEN=
