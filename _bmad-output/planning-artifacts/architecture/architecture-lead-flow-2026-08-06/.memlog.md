---
scope: Primer bloque de LeadFlow: núcleo brownfield actual más Web Push, sincronización corporativa controlada y frontera manual de decisión de compra
purpose: Sustrato de construcción para futuras IAs y mantenedores, preservando comportamiento validado
altitude: feature
updated: 2026-08-06T14:15
---

- (direction by user) Coaching path seleccionado; la espina es el entregable base para futuras IAs y mantenedores, sin artefacto narrativo adicional por ahora.
- (constraint by coach) El PRD final, la UX final, project-context.md, el código y las migraciones brownfield son fuentes vinculantes; la arquitectura extiende la aplicación y no la reescribe.
- (decision by user) [ADOPTED] La espina gobierna todo el primer bloque del PRD: núcleo existente, Web Push server-side, sincronización corporativa controlada y registro manual de la decisión de compra. Binds: capacidades del primer bloque. Previene: que cada épica adopte una arquitectura incompatible.
- (decision by user) [ADOPTED] LeadFlow permanece un monolito modular server-first; dominio y casos de uso internos se separan de repositorios y adaptadores de proveedores. Binds: todas las capacidades. Previene: dividir prematuramente el sistema o acoplar el dominio a Next.js, Supabase, Evolution, Push o al sistema corporativo.
- (constraint by user) [ADOPTED] La evolución debe preservar los flujos existentes que ya funcionan y fueron probados; un requisito nuevo solo puede sustituir comportamiento de forma explícita, con migración y verificación proporcional. Binds: cambios brownfield. Previene: regresiones silenciosas.
- (decision by user) [ADOPTED] El MVP opera con un único vendedor; autenticación y aislamiento multi-tenant quedan diferidos. Se conservan user_id y tenant_id como compatibilidad preparatoria, sin convertirlos en alcance ni crear una segunda semántica de propiedad. Binds: identidad, RLS y consultas. Previene: seguridad multi-tenant incompleta y filtros inconsistentes.
- (question by coach) Definir la autoridad y mecanismo de programación para Push server-side, incluidos deduplicación, reintentos y acciones desde notificaciones.
- (question by coach) Definir el adaptador de sincronización corporativa y dónde vive/protege su sesión, con confirmación humana e idempotencia.
- (version by coach) Next.js 16.2.12 está fijado en el repositorio; su guía local y la guía oficial vigente de PWA soportan manifest, Service Worker, Push API y envío Web Push desde servidor. La suscripción debe persistirse en base de datos para producción.
- (direction by coach) Web Push debe usar mejora progresiva: Push API y recepción mediante Service Worker son ampliamente soportadas, pero las acciones directas de notificación no son Baseline; siempre debe existir fallback que abra el detalle de la acción.
- (version by coach) Supabase vigente ofrece Cron sobre pg_cron, Queues durables sobre pgmq y Edge Functions TypeScript/Deno; las funciones son apropiadas para operaciones cortas e idempotentes y los trabajos pesados deben salir a workers.
- (direction by coach) Candidato para Push: Postgres conserva suscripción, intención, idempotency key y resultado; Cron despierta un dispatcher server-side. No usar el ciclo de vida de una petición Next.js ni Realtime como scheduler.
- (constraint by coach) Playwright puede reutilizar storageState, pero ese estado contiene cookies/credenciales capaces de impersonar la cuenta y nunca puede versionarse. Un adaptador de navegador requeriría aislamiento y almacenamiento secreto fuera del repositorio.
- (constraint by user) [ADOPTED] El sistema corporativo es accesible desde internet y autentica únicamente con correo y contraseña; no depende de VPN ni MFA. Binds: despliegue y recuperación de sesión del adaptador corporativo. Previene: diseñar una interacción local o humana innecesaria para cada ejecución.
- (direction by coach) Candidato corporativo: Playwright en un worker Node/Chromium aislado pero dentro del mismo repositorio y despliegue; cada intento confirmado usa un BrowserContext nuevo, inicia sesión con secretos server-side, ejecuta una sola operación, valida el resultado y cierra el contexto. No persistir storageState en el piloto.
- (direction by coach) Si la sesión expira o la interfaz no coincide con el contrato esperado, el intento termina en SESSION_EXPIRED o NEEDS_REVIEW; nunca reintenta la escritura automáticamente y exige nueva vista previa y confirmación.
- (decision by user) [ADOPTED] Push usa PostgreSQL como autoridad para suscripciones, intenciones, claves de idempotencia, intentos y resultados; Supabase Cron despierta una Edge Function de despacho. El Service Worker presenta la notificación y las acciones directas son mejora progresiva con fallback al detalle. Binds: FR-018 a FR-025 e instrumentación Push. Previene: depender de la PWA abierta, Realtime o una petición Next.js como scheduler, y confundir aceptación con entrega.
- (decision by user) [ADOPTED] La sincronización corporativa inicia con un adaptador Playwright; las solicitudes HTTP internas quedan diferidas detrás de CorporateLeadSyncPort hasta que un spike pruebe estabilidad y autorización. Binds: FR-026 a FR-033. Previene: acoplar el dominio a selectores o a un protocolo no oficial.
- (version by coach) Playwright 1.62.0 fue verificado como versión vigente: soporta ejecución headless en Ubuntu 22.04, 24.04 y 26.04 y exige alinear versión del paquete con binarios/imagen del navegador.
- (direction by coach) Despliegue recomendado para el piloto: worker Playwright headless en contenedor Ubuntu/Debian separado dentro del mismo Docker Compose y host; sin puerto público, concurrencia uno, Chromium y Playwright fijados a la misma versión, suficiente memoria compartida y límites de recursos. El contrato permite mover el worker a otro host sin cambiar Next.js ni el dominio.
- (constraint by coach) El worker inicia un BrowserContext limpio por intento, autentica con secretos server-side, nunca persiste storageState, y ante resultado externo incierto termina NEEDS_REVIEW sin reintentar automáticamente. Binds: seguridad e idempotencia corporativa. Previene: filtración de sesión y registros duplicados tras fallos parciales.
- (constraint by user) [ADOPTED] El sistema corporativo no ofrece ambiente ni cuenta de pruebas; cualquier escritura de validación ocurre en producción. Binds: pruebas, despliegue y habilitación del adaptador corporativo. Previene: tratar una prueba destructiva como ensayo seguro.
- (direction by coach) Candidato de rollout corporativo: fixtures y servidor simulado para todas las ramas; modo shadow en producción que inicia sesión, navega, prepara y compara la vista previa pero nunca pulsa guardar; luego un único canario autorizado con feature flag, evidencia y verificación automática y humana.
- (constraint by coach) La automatización corporativa permanece deshabilitada por defecto. Un fallo, selector inesperado o resultado incierto abre NEEDS_REVIEW y bloquea nuevos intentos; no existe retry automático de escrituras.
- (question by coach) Confirmar si el usuario podrá autorizar un único lead real como canario de producción cuando la implementación haya superado fixtures y shadow mode.
- (constraint by user) [ADOPTED] La operación corporativa inicial todavía no está definida y las primeras historias mejorarán otras capacidades de LeadFlow; no se implementarán ahora credenciales, worker, selectores, esquema de jobs ni flujo de escritura corporativa.
- (assumption by user) [USER-REPORTED, TO VERIFY] El sistema corporativo permite a cada asesor mantener un solo lead activo y bloquea registrar otro hasta finalizar el anterior. Debe verificarse con el asesor y mapear qué significa activo/finalizado antes de diseñar automatización.
- (decision by user) SUPERCEDES la adopción previa de Playwright como implementación inicial: la automatización corporativa queda diferida. Solo queda vinculante el límite CorporateAutomationPort y la máquina de estados preview-confirmación-ejecución-verificación para cualquier solución futura; Playwright y HTTP permanecen candidatos, no stack.
- (question by user) Revisitar automatización corporativa después de la reunión cuando existan credenciales, mapa de flujos permitidos, regla exacta de un lead activo, transición de finalización, operación prioritaria, evidencia de autorización y estrategia de validación en producción.
- (event by coach) La arquitectura vuelve a enfocarse en el núcleo y mejoras definidas de LeadFlow; la automatización corporativa no bloquea las primeras historias.
- (constraint by coach) EVIDENCE: las migraciones actuales conceden al rol anon lectura y mutación sobre leads, lead_messages y lead_follow_up_actions con user_id null; NEXT_PUBLIC_SUPABASE_URL y NEXT_PUBLIC_SUPABASE_ANON_KEY se incluyen en el cliente para Realtime. En un despliegue público esto no aísla PII ni mutaciones del único vendedor.
- (question by coach) Decidir si el piloto activa Supabase Auth con una sola cuenta persistente del asesor, migrando las filas null a su user_id y retirando políticas anon de datos privados, mientras catálogo público permanece anon.
- (direction by coach) Recomendación: mantener vendedor único pero exigir una sesión Supabase Auth; la PWA conserva la sesión y Realtime sigue directo con JWT. No convertir esto en multi-tenant ni añadir roles/equipos.
- (decision by user) [ADOPTED] Supabase Auth con correo y contraseña para el único asesor; las tablas privadas dejan de admitir acceso anon y aplican user_id = auth.uid().
- (decision by coach) [ADOPTED] Migración transaccional de seguridad: crear o identificar primero la cuenta del asesor, asignar a ese user_id las filas privadas con user_id null, verificar conteos y luego retirar políticas y grants anon; abortar si falta la identidad destino. Catálogo e imágenes pueden seguir públicos.
- (decision by coach) [ADOPTED] PostgreSQL/Supabase es la autoridad de estado. lead_follow_up_actions posee el ciclo de próxima acción; leads.next_action_at y leads.next_action_type son una proyección de lectura mantenida atómicamente por base de datos.
- (decision by coach) [ADOPTED] La respuesta entrante converge mediante upsert a una sola acción abierta RESPONSE por lead, protegida por restricción única parcial; actualiza vencimiento y resumen sin cancelar otras acciones no relacionadas.
- (decision by coach) [ADOPTED] Componentes UI no escriben persistencia directamente: Server Actions, Route Handlers y Edge Functions validan y autorizan, delegan a casos de uso; transiciones críticas y evento de auditoría se confirman juntos mediante RPC o transacción.
- (decision by coach) [ADOPTED] Todo efecto externo persiste primero un intento con clave de idempotencia única, ejecuta el proveedor y registra resultado; WhatsApp, Push y futuras integraciones reutilizan la misma clave al reintentar.
- (decision by coach) [ADOPTED] Web Push usa suscripciones y entregas durables en Postgres; Supabase Cron invoca una Edge Function que reclama vencimientos atómicamente. Service Worker muestra la notificación; acciones directas son mejora progresiva con fallback a la PWA.
- (decision by coach) [ADOPTED] leadflow_events es append-only para trazabilidad y métricas, con sobre estándar y payload mínimo sin secretos ni payload proveedor completo; no sustituye al estado operacional.
- (decision by coach) [ADOPTED] La decisión de compra es un hito manual explícito e idempotente, no se infiere de mensajes ni sobrecarga LeadStatus.
- (decision by coach) [ADOPTED] Evolución brownfield mediante migraciones aditivas numeradas, backfill y verificación antes de retirar rutas antiguas; nuevas salidas externas usan flags server-side apagados por defecto hasta validación.
- (decision by coach) [SUPERSEDED] La adopción previa de Playwright queda revocada. Automatización corporativa, adaptador, runtime y credenciales quedan Deferred hasta reunión con el asesor, definición de flujos permitidos y verificación de la regla de un solo lead activo.
- (decision by coach) [ADOPTED] Se persisten timestamps como timestamptz/UTC y se interpretan o muestran en America/Guayaquil; Realtime solo invalida/refresca proyecciones y nunca ejecuta planificación ni efectos externos.
- (evidence by coach) Verificación 2026-08-06: Ubuntu 24.04 LTS mantiene soporte estándar hasta mayo de 2029; se elige como baseline madura del host en lugar de exigir interfaz gráfica.
- (evidence by coach) Verificación 2026-08-06: Supabase documenta Cron sobre pg_cron, historial en cron.job_run_details e invocación periódica de Edge Functions con secretos en Vault.
- (evidence by coach) Verificación 2026-08-06: W3C Push API mantiene el modelo servidor de aplicación -> push service -> Service Worker incluso con la aplicación inactiva.
- (evidence by coach) Verificación 2026-08-06: Evolution API v2.3.7 existe como release oficial y coincide con el tag fijado en Docker Compose.
- (decision by coach) [ADOPTED] Despliegue de producción del piloto: Ubuntu Server 24.04 LTS sin escritorio, Docker Compose para Next standalone, Evolution API y Redis, Supabase administrado para Auth/Postgres/Realtime/Cron/Edge Functions; HTTPS y backups son precondiciones operativas.
- (decision) [ADOPTED] Round-two gate integration binds every advisor action mutation to monotonic action_version and RESPONSE source_message_id; stale commands are no-ops so newer inbound work cannot be closed.
- (decision) [ADOPTED] External effects separate one logical effect from immutable attempt identities and evidence; late definitive results reconcile only the latest attempt, UNKNOWN never resends without proof, and no retry creates a new effect identity.
- (decision) [ADOPTED] Push uses same-origin credentialed POST with body-only one-use capability and no post-login auto-execution; subscription generation changes atomically materialize or cancel versioned deliveries and database-owned encryption prevents cross-runtime ciphertext drift.
- (decision) [ADOPTED] The database singleton leadflow_installation is the only advisor identity authority; runtime-specific Supabase keys, scheduler authentication tests, host/Edge secret authorities and a mandatory private-port digest-pinned production Compose artifact govern deployment.
- (decision) [ADOPTED] Push dispatch runs at most every 20 seconds with a 10-second start budget, warning at 40 seconds and critical alert at 55 seconds against the PRD 60-second request-start boundary.
- (decision) [ADOPTED] leadflow_event_registry version 1 is the sole event vocabulary with deterministic unique event keys; corporate wildcard events remain non-emittable until discovery reopens the architecture.
- (decision) [ADOPTED] Next-action projection ties order by scheduled_for, created_at and id; DELIVERY is unique per lead; production host secrets use root-owned 0600 files and Edge/Cron secrets use Supabase project secrets or Vault.
- (decision) El capability compartido Effects es el único dueño de external_effects y external_effect_attempts; los agregados de WhatsApp y Push tienen referencias uno-a-uno al efecto canónico y no mantienen máquinas de efecto paralelas.
- (decision) Todo adaptador debe obtener un fence pre-I/O mediante begin_effect_io_v1: el CAS bloquea el efecto, verifica claim actual, intento más reciente, token, lease vigente y request_started_at nulo, y debe confirmar antes de cualquier llamada de red; el reaper usa el mismo bloqueo.
- (decision) Las mutaciones de acciones y generaciones de suscripción serializan por la fila singleton del asesor, siguen un orden fijo de locks y materializan o reparan el postestado completo de acciones abiertas por suscripciones activas.
- (decision) La identidad singleton del asesor queda inmutable después del corte Auth; se revocan updates runtime y cualquier reemplazo exige una futura migración de mantenimiento que reasigne todas las raíces de propiedad de forma auditada.
- (decision) Cada evento TRANSITION lleva aggregate_type, aggregate_id y aggregate_version canónicos; las RPC dueñas incrementan la versión del agregado y anexan el evento en la misma transacción.
- (decision) Push distingue push_delivery_scheduled de push_generated: solo acciones abiertas materializan entregas, la generación ocurre al comenzar I/O debido, y capacidades por comando se emiten por intento, se guardan solo como digest en servidor y como token bruto solo dentro del payload Push cifrado y Notification.data.
- (event) Final reviewer-gate findings integrated; spine remains draft; lint_spine.py passed with zero findings.
- (event by coach) Reviewer Gate final: lint PASS, verificación tecnológica PASS, rubric walker PASS y revisión adversarial PASS; sin hallazgos críticos ni altos.
- (event by coach) spine finalized
